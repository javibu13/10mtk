class_name Enums
extends RefCounted

enum Character {
	TIGER,
	MOUSE,
	
}

enum TileType {
	COMMON,
	SNIPER,
}

enum PlayerStatus {
	LIVE,
	DEAD,
	ARRESTED
}

enum Action {
	MOVE,
	KILL,
	ASK,
}

enum ActionNumber {
	FIRST,
	SECOND
}
